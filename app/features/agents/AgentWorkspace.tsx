"use client";
/* eslint-disable react-hooks/set-state-in-effect, react-hooks/exhaustive-deps */

import { useEffect, useMemo, useState } from "react";

type Flag = { nome: string; ativo: boolean };
type Execution = { id: number; automacao_nome: string | null; evento: string | null; status: string | null; detalhe: string | null; lead_nome: string | null; criado_em: string | null };
type CenterData = { flags: Flag[]; executions: Execution[]; error?: string };
type Agent = { name: string; role: string; objective: string; audience: string; tone: string; behavior: string; examples: string[]; expected: string[]; forbidden: string[]; active: boolean; version: string; color: string; symbol: string };

const agents: Agent[] = [
  { name: "Assistente do Sistema", role: "Ensina a usar o ERP, explica campos e abre o módulo certo.", objective: "Reduzir dúvidas operacionais e acelerar o uso do sistema.", audience: "Toda a equipe", tone: "Didático e direto", behavior: "Responde com passo a passo e leva o usuário à tela certa.", examples: ["Como agendo uma visita?", "Onde vejo minha comissão?", "Como altero a etapa de um lead?"], expected: ["Passo a passo claro", "Link para a tela correta"], forbidden: ["Inventar telas que não existem", "Dar conselho financeiro"], active: true, version: "v3", color: "#2f6fed", symbol: "?" },
  { name: "Copiloto de Atendimento", role: "Sugere e melhora mensagens, responde objeções e qualifica no chat.", objective: "Elevar a qualidade e a velocidade do atendimento.", audience: "Corretores", tone: "Caloroso e comercial", behavior: "Personaliza pelo nome, faz perguntas e conduz para a visita.", examples: ["Sugira uma resposta", "Responda à objeção de preço", "Tente agendar visita"], expected: ["Mensagem personalizada", "Chamada para ação"], forbidden: ["Prometer desconto não autorizado", "Enviar apenas a tabela"], active: true, version: "v5", color: "#25a85a", symbol: "C" },
  { name: "Avaliador de Atendimento", role: "Analisa conversas, dá nota e aponta oportunidades de melhoria.", objective: "Medir e melhorar a qualidade do atendimento.", audience: "Gestores", tone: "Analítico e imparcial", behavior: "Pontua velocidade, clareza, condução e gera recomendações.", examples: ["Avalie esta conversa", "Quais erros mais comuns?", "Quem precisa de treino?"], expected: ["Nota com composição", "Recomendações objetivas"], forbidden: ["Julgar sem dados", "Expor o corretor publicamente"], active: true, version: "v2", color: "#8b00cc", symbol: "A" },
  { name: "Treinador Comercial", role: "Recomenda treinamentos e cria um plano de melhoria por corretor.", objective: "Desenvolver a equipe a partir dos pontos fracos.", audience: "Gestores e corretores", tone: "Encorajador", behavior: "Cruza esforço e resultado e sugere exercícios práticos.", examples: ["Plano de melhoria para o corretor", "Qual treino priorizar?"], expected: ["Plano acionável", "Exercícios simulados"], forbidden: ["Metas irreais", "Comparações desmotivadoras"], active: false, version: "v1", color: "#d49a00", symbol: "T" },
  { name: "Especialista em Produtos", role: "Busca imóveis, compara unidades e sugere argumentos.", objective: "Ajudar a encontrar e apresentar o imóvel certo.", audience: "Corretores", tone: "Consultivo", behavior: "Filtra pelo perfil do lead e indica alternativas semelhantes.", examples: ["2 dorms prontos até 700 mil", "Qual combina com este lead?", "Quais materiais enviar?"], expected: ["Lista com preço/m²", "Alternativa semelhante"], forbidden: ["Recomendar fora do orçamento", "Dado de estoque inventado"], active: true, version: "v4", color: "#ff7000", symbol: "P" },
  { name: "Especialista em Financiamento", role: "Explica etapas, documentos e interpreta simulações.", objective: "Tornar o crédito imobiliário simples e guiado.", audience: "Corretores e clientes", tone: "Claro e tranquilizador", behavior: "Explica parcela e entrada e prepara mensagem para o cliente.", examples: ["Quais documentos faltam?", "Explique a simulação", "Qual o melhor banco?"], expected: ["Explicação simples", "Comparação de bancos"], forbidden: ["Garantir aprovação", "Aconselhar sonegação"], active: true, version: "v3", color: "#0e9488", symbol: "$" },
  { name: "Gestor de Carteira", role: "Encontra leads atrasados, prioriza contatos e organiza o dia.", objective: "Aproveitar melhor a base e reduzir leads parados.", audience: "Corretores", tone: "Objetivo e prático", behavior: "Ranqueia por urgência e sugere a próxima tarefa.", examples: ["Quem está esperando resposta?", "Meu plano de hoje", "Leads quentes parados"], expected: ["Lista priorizada", "Próxima ação por lead"], forbidden: ["Ignorar SLA", "Sugerir spam"], active: true, version: "v2", color: "#cc5800", symbol: "G" },
  { name: "Gestor da Operação", role: "Analisa corretores, prevê resultado e sugere decisões.", objective: "Dar ao gestor visão de risco e capacidade da equipe.", audience: "Gestores e diretores", tone: "Estratégico", behavior: "Identifica gargalos, risco de meta e sobrecarga.", examples: ["Quem corre risco de meta?", "Qual o gargalo do funil?", "Quem pode receber mais leads?"], expected: ["Diagnóstico com números", "Recomendação de ação"], forbidden: ["Decisão de RH", "Exposição indevida de dados"], active: true, version: "v4", color: "#66009a", symbol: "O" },
];

export function AgentWorkspace({ accessToken }: { accessToken: string }) {
  const [data, setData] = useState<CenterData>({ flags: [], executions: [] });
  const [selected, setSelected] = useState<Agent | null>(null);
  const [loading, setLoading] = useState(true);
  const [notice, setNotice] = useState("");
  async function load() { setLoading(true); const response = await fetch("/api/ai-center", { headers: { Authorization: `Bearer ${accessToken}` } }); const body = await response.json() as CenterData; setData(body); setLoading(false); }
  useEffect(() => { void load(); }, [accessToken]);
  const flagMap = useMemo(() => new Map(data.flags.map((flag) => [flag.nome.replace(/^agente:/, ""), flag.ativo])), [data.flags]);
  const rendered = agents.map((agent) => ({ ...agent, active: flagMap.has(agent.name) ? Boolean(flagMap.get(agent.name)) : agent.active }));
  async function toggle(agent: Agent, active: boolean) { setNotice(""); const response = await fetch("/api/ai-center", { method: "PATCH", headers: { Authorization: `Bearer ${accessToken}`, "Content-Type": "application/json" }, body: JSON.stringify({ action: "toggleAgent", name: agent.name, active }) }); const body = await response.json() as { error?: string }; if (!response.ok) setNotice(body.error ?? "Não foi possível salvar."); else { setNotice(`${agent.name} ${active ? "ativado" : "pausado"}.`); await load(); setSelected((current) => current?.name === agent.name ? { ...current, active } : current); } }
  return <div className="agent-workspace"><header className="workspace-top"><div><h1>Agentes de IA</h1><p>Agentes especializados para apoiar a operação imobiliária</p></div><span className="agent-total">{rendered.filter((agent) => agent.active).length} ativos</span></header>{notice && <div className="agent-notice">{notice}</div>}{loading ? <div className="workspace-loading">Carregando agentes...</div> : data.error ? <div className="workspace-error">{data.error}</div> : <main className="agent-grid">{rendered.map((agent) => <article className="agent-card" key={agent.name}><div className="agent-card-head"><span style={{ background: agent.color }}>{agent.symbol}</span><div><h2>{agent.name}</h2><p>{agent.role}</p></div><label className="agent-switch"><input type="checkbox" checked={agent.active} onChange={(event) => void toggle(agent, event.target.checked)} /><i /></label></div><div className="agent-stats"><span><b>0</b> fontes</span><span><b>{data.executions.filter((item) => item.automacao_nome === agent.name).length}</b> execuções</span><mark className={agent.active ? "active" : "paused"}>{agent.active ? "Ativo" : "Pausado"}</mark></div><footer><small>{agent.version}</small><button type="button" onClick={() => setSelected(agent)}>Configurar e testar</button></footer></article>)}</main>}{selected && <div className="agent-layer" onClick={() => setSelected(null)}><aside className="agent-drawer" onClick={(event) => event.stopPropagation()}><header><span style={{ background: selected.color }}>{selected.symbol}</span><div><h2>{selected.name}</h2><p>{selected.role}</p></div><button type="button" onClick={() => setSelected(null)}>×</button></header><section><div className="agent-definition"><small>OBJETIVO</small><p>{selected.objective}</p><small>PÚBLICO</small><p>{selected.audience}</p><small>TOM</small><p>{selected.tone}</p><small>COMPORTAMENTO</small><p>{selected.behavior}</p></div><h3>Perguntas de teste</h3><div className="agent-prompts">{selected.examples.map((item) => <button type="button" onClick={() => setNotice(`Teste preparado: “${item}”. A resposta real depende da chave do provedor de IA.`)} key={item}>{item}</button>)}</div><div className="agent-rules"><div><h3>Respostas esperadas</h3>{selected.expected.map((item) => <p key={item}>✓ {item}</p>)}</div><div><h3>O agente não pode</h3>{selected.forbidden.map((item) => <p key={item}>× {item}</p>)}</div></div><h3>Execuções reais recentes</h3><div className="agent-history">{data.executions.slice(0, 8).map((item) => <article key={item.id}><b>{item.automacao_nome || item.evento || "Motor de IA"}</b><span>{item.status || "registrada"} · {item.criado_em ? new Date(item.criado_em).toLocaleString("pt-BR") : "—"}</span><p>{item.detalhe || item.lead_nome || "Sem detalhe adicional"}</p></article>)}{!data.executions.length && <p>Nenhuma execução registrada no motor.</p>}</div></section><footer><label className="agent-switch-row"><span><b>{selected.active ? "Agente ativo" : "Agente pausado"}</b><small>O estado é salvo no Supabase.</small></span><input type="checkbox" checked={selected.active} onChange={(event) => void toggle(selected, event.target.checked)} /></label></footer></aside></div>}</div>;
}
